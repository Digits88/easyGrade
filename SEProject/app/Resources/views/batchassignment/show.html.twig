{% extends 'base.html.twig' %}

{% block body %}
    <h1>BatchAssignment</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ batchAssignment.id }}</td>
            </tr>
            <tr>
                <th>Batchid</th>
                <td>{{ batchAssignment.batchid }}</td>
            </tr>
            <tr>
                <th>Assignmentid</th>
                <td>{{ batchAssignment.assignmentid }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('batchassignment_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('batchassignment_edit', { 'id': batchAssignment.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
